export type PriceInterface = { // TODO:: move all types to common folder
  IMDB: string;
  priceId?: string;
  regions?: string[];
  medium?: string;
  fromWindow?: string;
  toWindow?: string;
  amount?: string;
};
